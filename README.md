# Octop Pet

Desktop pet built with Tauri 2, React, and TypeScript.

## Development

Install the [Tauri prerequisites](https://tauri.app/start/prerequisites/), then run:

```sh
npm install
npm run tauri dev
```

Run the test suite with:

```sh
npm test
```
